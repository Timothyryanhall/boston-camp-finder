export default function FinderPage() {
  return (
    <section className="rounded-[32px] border border-sand-200 bg-white/85 p-8 shadow-card backdrop-blur">
      <div className="max-w-3xl">
        <p className="text-sm font-semibold uppercase tracking-[0.22em] text-teal-800">
          Route shell
        </p>
        <h2 className="mt-3 text-4xl font-black tracking-tight text-sand-900">
          Boston Camp Finder
        </h2>
        <p className="mt-4 text-base leading-7 text-sand-700">
          The typed Vite app shell is in place. Finder data loading and filter
          interactions will layer onto this route in later tasks.
        </p>
      </div>
    </section>
  );
}
